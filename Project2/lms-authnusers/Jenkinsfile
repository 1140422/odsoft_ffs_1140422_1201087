pipeline {
  agent any

  tools {
    maven 'Maven3'
    jdk 'Java21'
  }

  environment {
    APP_PATH = "Project2/lms-authnusers"
    DOCKER_IMAGE = "lmsusers"
    DOCKER_REPO = "1140422/lmsusers"
    DOCKER_TAG = "${BUILD_NUMBER}"
  }

  stages {

    stage('Checkout') {
        steps {
            checkout scm
        }
    }

            stage('Build') {
                steps {
                    dir("${APP_PATH}") {
                        script {
                            if (isUnix()) {
                                sh 'mvn clean compile -DskipTests'
                            } else {
                                bat 'mvn clean compile -DskipTests'
                            }
                        }
                    }
                }
            }

            stage('Static Code Analysis') {
                steps {
                    dir('Project2/lms-authnusers') {
                        script {
                            if (isUnix()) {
                                sh 'mvn checkstyle:check spotbugs:spotbugs -DskipTests'
                            } else {
                                bat 'mvn checkstyle:check spotbugs:spotbugs -DskipTests'
                            }
                        }
                    }
                }
                post {
                    always {
                        recordIssues tools: [
                            checkStyle(pattern: 'Project2/lms-authnusers/target/checkstyle-result.xml'),
                            spotBugs(pattern: 'Project2/lms-authnusers/target/spotbugsXml.xml')
                        ]
                    }
                }
            }

            stage('Packaging') {
                steps {
                    dir('Project2/lms-authnusers') {
                        script {
                            if (isUnix()) {
                                sh 'mvn package -DskipTests'
                            } else {
                                bat 'mvn package -DskipTests'
                            }
                        }
                    }
                }
            }

            stage('Unit Tests') {
                steps {
                    dir('Project2/lms-authnusers') {
                        script {
                            if (isUnix()) {
                                sh 'mvn test'
                            } else {
                                bat 'mvn test'
                            }
                        }
                    }
                }
                post {
                    always {
                        junit 'Project2/lms-authnusers/target/surefire-reports/*.xml'
                        recordCoverage(tools: [[parser: 'JACOCO']],
                            id: 'jacoco', name: 'JaCoCo Coverage',
                            sourceCodeRetention: 'EVERY_BUILD',
                            qualityGates: [
                                [threshold: 10.0, metric: 'LINE', baseline: 'PROJECT', unstable: true],
                                [threshold: 2.0, metric: 'BRANCH', baseline: 'PROJECT', unstable: true]
                            ])
                    }
                }
            }

            stage('Mutation Tests') {
                steps {
                    dir('Project2/lms-authnusers') {
                        script {
                            if (isUnix()) {
                                sh 'mvn pitest:mutationCoverage'
                            } else {
                                bat 'mvn pitest:mutationCoverage'
                            }
                        }
                    }
                }
                post {
                    always {
                        publishHTML(target: [
                            allowMissing: true,
                            keepAll: true,
                            reportDir: 'Project2/lms-authnusers/target/pit-reports',
                            reportFiles: 'index.html',
                            reportName: 'Mutation Testing Report'
                        ])
                    }
                }
            }

        stage('Container image build') {
            steps {
                dir('Project2/lms-authnusers') {
                    script {
                        if (isUnix()) {
                          sh "docker build -t ${DOCKER_IMAGE}:${DOCKER_TAG} ."
                          sh "docker build -t $DOCKER_IMAGE:latest ."
                        } else {
                            bat "docker build -t %DOCKER_IMAGE%:%DOCKER_TAG% ."
                        }
                    }
                }
            }
        }

        stage('Docker Login & Push') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub-credentials',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh """
                        docker login -u $DOCKER_USER -p $DOCKER_PASS
                        docker tag $DOCKER_IMAGE:latest $DOCKER_REPO:$DOCKER_TAG
                        docker tag $DOCKER_IMAGE:latest $DOCKER_REPO:latest
                        docker push $DOCKER_REPO:$DOCKER_TAG
                        docker push $DOCKER_REPO:latest
                    """
                }
            }
        }

        stage('Staging Deployment (Local)') {
            steps {
                dir('Project2/lms-authnusers') {
                    sh """
                        echo "Stopping any existing staging containers..."
                        docker compose down || true
                        
                        echo "Starting staging environment locally..."
                        docker compose up -d --build
                        
                        echo "Listing running containers for staging..."
                        docker ps
                    """
                }
            }
            post {
                always {
                    echo "Local staging deployment attempted."
                }
                success {
                    echo "Local staging environment is up and running!"
                }
                failure {
                    echo "Local staging deployment failed — check logs."
                }
            }
        }

        stage('Deploy to Remote Server') {
            steps {
                sshagent(credentials: ['home-server-ssh']) {
                sh """
                    scp -o StrictHostKeyChecking=no Project2/lms-authnusers/docker-compose.prod.yml nuno@192.168.1.200:/opt/lms/docker-compose.prod.yml
                    ssh -o StrictHostKeyChecking=no nuno@192.168.1.200 'cd /opt/lms && docker compose -f docker-compose.prod.yml pull && docker compose -f docker-compose.prod.yml up -d'
                """
                }
            }
        }
  }

  post {
    success {
      echo "Pipeline OK - image: ${DOCKER_IMAGE}:${DOCKER_TAG}"
    }
    failure {
      echo "Pipeline failed — check logs"
    }
  }
}